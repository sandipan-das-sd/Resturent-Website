# Aaranya Restaurant Demo

A responsive one-page restaurant concept built with plain HTML, CSS, and JavaScript.

## Preview

Open `index.html` directly, or run a local static server from this directory:

```powershell
npx serve .
```

The concept includes a responsive editorial layout, animated sections, tabbed menus, and a working reservation dialog (demo only).
